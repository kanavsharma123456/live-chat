import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'tele_health_nurse',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
